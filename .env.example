BOT_TOKEN=your_bot_token_from_botfather
# Для супергруппы ID вида -100… (как в @GetIDsBot). Можно указать без минуса — в коде будет исправлено.
ADMIN_GROUP_CHAT_ID=-1001234567890
# Каталог для allowed_users.json и sessions.json (на Timeweb — persistent volume). По умолчанию ./data
# DATA_DIR=/mnt/tw/data
# Необязательно: свой путь к recipes.json (по умолчанию data/recipes.json из образа)
# RECIPES_PATH=/app/data/recipes.json

# FastAPI + Telegram webhook
# Укажите публичный HTTPS-адрес приложения без слеша на конце.
WEBHOOK_BASE_URL=https://your-domain.example
# Или полный URL webhook вместо WEBHOOK_BASE_URL:
# WEBHOOK_URL=https://your-domain.example/telegram/webhook
WEBHOOK_PATH=/telegram/webhook
# Необязательный секрет, Telegram будет присылать его в заголовке.
# WEBHOOK_SECRET_TOKEN=change-me
# PORT обычно задаёт хостинг. Локально можно оставить 8000.
# PORT=8000
